# REEDS EC2 worker environment template.
# Fill in real values and EXPORT them (or place at /etc/reeds.env and use as
# the systemd EnvironmentFile). NEVER commit real values to the repository.
#
#   export AIVEN_DATABASE_URL='postgresql://...'    # Aiven production DB (primary)
#   export DATABASE_URL='postgresql://...'          # legacy fallback; same value as
#                                                   # AIVEN_DATABASE_URL is fine
#   export ADMIN_API_KEY='...'                      # admin key for /api/admin/* endpoints
#   export GITHUB_TOKEN='...'                       # Contents: read/write on the REEDS repo
#   export GITHUB_REPO='zagzy8776/REEDS'
#   export MODEL_DIR='/home/ubuntu/REEDS/backend/data/models'
#   export MIN_TRAINING_ROWS='200'
#   # export SPORTS_OVERRIDE='soccer,basketball'    # optional run-all subset

AIVEN_DATABASE_URL=
DATABASE_URL=
ADMIN_API_KEY=
GITHUB_REPO=zagzy8776/REEDS
MODEL_DIR=/home/ubuntu/REEDS/backend/data/models
MIN_TRAINING_ROWS=200
# SPORTS_OVERRIDE=