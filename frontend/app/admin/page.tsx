export default function Admin() {
  return <main className="mx-auto max-w-4xl px-6 py-10"><h1 className="text-4xl font-black">Owner Control Room</h1><p className="mt-2 text-slate-400">This is where the LOYAL EDGE model is trained, refreshed, and prepared for customers.</p><div className="mt-8 card"><p className="text-slate-300">Use backend admin endpoints with your private <code>X-Admin-Key</code> header:</p><pre className="mt-4 overflow-auto rounded-xl bg-black p-4 text-xs text-emerald-300">POST /api/admin/train{"\n"}POST /api/admin/predict</pre><p className="mt-4 text-sm text-slate-500">For security, this MVP does not expose admin keys in the browser.</p></div><div className="mt-5 card"><h2 className="text-xl font-bold">Model Feeding Plan</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-300"><li>Load historical football CSVs from football-data.co.uk and footballcsv datasets.</li><li>Load NBA/basketball historical CSVs from Kaggle or GitHub datasets.</li><li>Retrain after every major data upload or weekly refresh.</li><li>Generate fresh predictions only after training completes.</li></ul></div></main>;
}
